<?xml version="1.0" encoding="utf-8"?>
<shortcuts
    xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:enabled="true"
        android:icon="@drawable/ic_notification_shortcut"
        android:shortcutDisabledMessage="@string/shortcut_long_label"
        android:shortcutId="open_notification"
        android:shortcutLongLabel="@string/shortcut_long_label"
        android:shortcutShortLabel="@string/shortcut_short_label">
        <intent
            android:action="com.sihamark.pokemonlist.SHOW_NOTIFICATION"
            android:targetClass="com.sihamark.pokemonlist.ui.MainActivity"
            android:targetPackage="com.sihamark.pokemonlist" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
        <categories android:name="android.shortcut.conversation" />

    </shortcut>
</shortcuts>
